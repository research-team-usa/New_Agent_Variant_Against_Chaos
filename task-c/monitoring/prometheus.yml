prometheus:
  image: prom/prometheus
  container_name: prometheus
  volumes:
    - ./task-c/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
  ports:
    - "9090:9090"
  networks:
    - agent_net
